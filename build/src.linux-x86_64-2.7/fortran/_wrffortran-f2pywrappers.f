C     -*- fortran -*-
C     This file is autogenerated with f2py (version:2)
C     It contains Fortran 77 wrappers to fortran functions.

      subroutine f2pywraptvirtual (tvirtualf2pywrap, temp, ratmix)
      external tvirtual
      real(kind=8) temp
      real(kind=8) ratmix
      real(kind=8) tvirtualf2pywrap, tvirtual
      tvirtualf2pywrap = tvirtual(temp, ratmix)
      end


      subroutine f2pywraptonpsadiabat (tonpsadiabatf2pywrap, thte,
     & prs, psadithte, psadiprs, psaditmk, gamma, errstat, errmsg)
      external tonpsadiabat
      real(kind=8) thte
      real(kind=8) prs
      real(kind=8) gamma
      integer errstat
      real(kind=8) psadithte(150)
      real(kind=8) psadiprs(150)
      real(kind=8) psaditmk(150,150)
      character*(*) errmsg
      real(kind=8) tonpsadiabatf2pywrap, tonpsadiabat
      tonpsadiabatf2pywrap = tonpsadiabat(thte, prs, psadithte, ps
     &adiprs, psaditmk, gamma, errstat, errmsg)
      end


      subroutine f2pywrapwrf_intrp_value (wrf_intrp_valuef2pywrap,
     & wvalp0, wvalp1, vlev, vcp0, vcp1, icase, errstat, errmsg)
      external wrf_intrp_value
      real(kind=8) wvalp0
      real(kind=8) wvalp1
      real(kind=8) vlev
      real(kind=8) vcp0
      real(kind=8) vcp1
      integer icase
      integer errstat
      character*(*) errmsg
      real(kind=8) wrf_intrp_valuef2pywrap, wrf_intrp_value
      wrf_intrp_valuef2pywrap = wrf_intrp_value(wvalp0, wvalp1, vl
     &ev, vcp0, vcp1, icase, errstat, errmsg)
      end

